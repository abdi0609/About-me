import { Typography } from "@mui/material";
import { ReactElement } from "react";
import * as React from "react";

export default function projectsAndTechStack(): ReactElement {
  return <Typography>projectsAndTechStack</Typography>;
}
